# Copy this file to ".env" at the repo root and fill in the key your instructor gave you.
# Used by the notebooks/ TD labs (python-dotenv walks up from any notebook to find it here).
# The .env file is gitignored — never commit your real key.
ANTHROPIC_API_KEY=sk-ant-your-key-here
