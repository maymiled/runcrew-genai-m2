# Copy this file to ".env" (same folder).
# The .env file is gitignored — never commit real values.

# URL of the deployed Coach IA backend (Render). Leave empty to fall back to
# http://localhost:5050 (see src/lib/coach.ts).
EXPO_PUBLIC_COACH_API_URL=https://runcrew-coach-ia.onrender.com
